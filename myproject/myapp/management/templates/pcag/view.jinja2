# coding: utf-8
from django.shortcuts import render_to_response, get_object_or_404, redirect


def {{ snake }}_view(request):

    empresa_logada = request.session['empresa_logada']

    usecase = {{ camel }}UsecaseFactory.make(empresa_logada.id)

    struct = {{ camel }}Struct(value1=10, value2=20)
    retorno = usecase.executar(struct)
    
    return render_to_response("template.html",
                              {
                                  'retorno': retorno
                              }
                              context_instance=RequestContext(request))
